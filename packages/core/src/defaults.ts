export const DEFAULT_COPILOT_MAX_TOKENS = 256 as const;
export const DEFAULT_COPILOT_STOP_SEQUENCE = '\n\n' as const;
export const DEFAULT_COPILOT_TEMPERATURE = 0.1 as const;
export const DEFAULT_COPILOT_TOP_P = 0.1 as const;
export const DEFAULT_COPILOT_STREAM = false as const;
